import colors from "./colors";


export const light = {
    mainBg: colors.lightBackground,
    textColor: colors.lightTextColor,
    inputBackground: colors.lightInputBackground,
    hintColor: colors.lightHintColor,
};

export const dark = {
    mainBg: colors.darkBackground,
    textColor: colors.darkTextColor,
    inputBackground: colors.darkInputBackground,
    hintColor: colors.darkHintColor,
};